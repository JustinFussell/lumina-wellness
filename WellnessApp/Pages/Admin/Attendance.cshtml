@page "{id:int}"
@model Lumina.Pages.Admin.AttendanceModel
@using Lumina.Data
@{
    ViewData["Title"] = "Take Attendance";
}

<div class="max-w-4xl mx-auto px-6 py-8">
    <h1 class="section-title text-3xl mb-2">Attendance</h1>
    <p class="mb-6 text-lumina-charcoal/70">@Model.ScheduledClass?.ClassType.Name with @Model.ScheduledClass?.Instructor.Name • @Model.ScheduledClass?.StartUtc.ToString("f")</p>

    <form method="post">
        <div class="lumina-card p-6">
            @if (Model.Bookings.Any())
            {
                <div class="space-y-4">
                    @foreach (var booking in Model.Bookings)
                    {
                        <div class="flex items-center justify-between border-b pb-3 last:border-b-0 last:pb-0">
                            <div>
                                <div class="font-medium">@booking.User.FullName</div>
                                <div class="text-xs text-lumina-charcoal/60">@booking.User.Email</div>
                            </div>
                            <div class="flex items-center gap-3">
                                <label class="flex items-center gap-2 text-sm">
                                    <input type="checkbox" name="attendedIds" value="@booking.Id" 
                                           checked="@(booking.Status == BookingStatus.Attended)" />
                                    Attended
                                </label>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <p>No bookings for this class.</p>
            }
        </div>

        <div class="mt-6 flex gap-4">
            <button type="submit" class="lumina-btn-primary">Save Attendance</button>
            <a asp-page="/Admin/Index" class="lumina-btn-ghost">Back to Dashboard</a>
        </div>
    </form>
</div>